const mongoose = require('mongoose');
console.log(`"agenda-service" is valid ObjectId?`, mongoose.Types.ObjectId.isValid("agenda-service"));
console.log(`"agenda-service" length:`, "agenda-service".length);
